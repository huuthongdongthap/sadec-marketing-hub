Fix the issue: "$ARGUMENTS"

Steps:
1. Identify the root cause by reading relevant files
2. Make the minimal fix needed
3. Verify the fix doesn't break anything (check related imports/references)
4. Commit with message: "fix: $ARGUMENTS"

Rules:
- Minimal changes only
- Don't refactor unrelated code
- Test after fixing
