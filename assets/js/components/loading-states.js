/**
 * ==============================================
 * MEKONG AGENCY - LOADING STATES (COMPONENTS)
 *
 * NOTE: This file re-exports from canonical source
 * src/js/components/loading-states.js to maintain backwards compatibility.
 *
 * All loading components are consolidated in one location.
 * ==============================================
 */

// Re-export all loading utilities from canonical source
export {
    LoadingManager,
    ProgressIndicator,
    LazyImageLoader,
    setButtonLoading,
    PageTransitionLoader,
    loading
} from '../../../src/js/components/loading-states.js';

// Re-export default
export { default } from '../../../src/js/components/loading-states.js';
